{% extends 'base.html.twig' %}

{% block title %}Автор{% endblock %}

{% block body %}
    <h1>Автор</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ author.id }}</td>
        </tr>
        <tr>
            <th>Имя</th>
            <td>{{ author.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}">Список авторов</a>

{% endblock %}
